1. remove value property from user configuration
2. use htmlFormat instead of htmlContent
3. Keep value as object always
4. Test htmlformat by specying any div html and one or two image tag/ some anchor tag as well
5. Use override config
6. expose all templates from inside the pager and let user override only parts that user wish to override.
7. expose placeholder map from inside this
8. add one more property "attr" this will set container element html attributes
9. Keep total records
10. Update the format for key value to use directly key as our own created key from Map of functions
11. Keep number of links configurable
12. Subscribe pagesize and current page values instead of calling onChange from each and every event
13. Keep a static variable to hold all pager instances
